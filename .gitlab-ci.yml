image: alpine:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive


pages:
  stage: deploy
  script:
  - mkdir .public && cp -r index.html *css *jpg lib-codemirror fonts  .public/ || true  && mv .public public
  only:
  - master
  artifacts:
    paths:
    - public
